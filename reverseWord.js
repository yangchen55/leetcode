// Write a function that takes an array of characters and reverses the letters in place. 

// const character = ["k", "A", "R", "M", "A"]
// const character = []
const character = ["K"]
const reverse = (arg) => {
    arg.reverse()
    console.log(arg)
}
reverse(character)